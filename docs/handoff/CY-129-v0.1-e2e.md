# CY-129 v0.1.0 Diagnostics E2E

Repeatable Phase A health-page acceptance. Summaries must not include novel body, full prompts, or unnecessary absolute paths.

## Environment

- Windows 11, `LNS_EXECUTION_PROFILE=cpu-dev` for suite; real GPU probe recorded separately as WINDOWS_GPU.
- Backend `127.0.0.1:8000`, frontend `127.0.0.1:5173`.
- Commit of this document.

## Checklist

| Scenario | How | Result (2026-09-21) |
| --- | --- | --- |
| SQLite healthy | Open `/diagnostics` | `data/app.db @ 0001_app_settings` ok |
| Ollama healthy | Same page while Ollama is running | `Ollama 0.33.2; qwen3.5:9b installed` |
| Ollama down | Unit: `test_collect_diagnostics_marks_missing_providers` | ollama `error`, page still renders |
| Model missing | Existing adapter/diagnostics warning path | warning + pull hint; no large pull on CPU_DEV |
| Milvus down | Stop stack, refresh health page | warning + rebuildable-index hint, no white screen |
| Milvus up | CY-125 smoke: healthz OK then Diagnostics milvus=`ok` | recorded in `docs/handoff/CY-125-milvus-smoke.md` |
| GPU ok | This Windows machine | RTX 5070 Ti 16303 MiB, `gpu_ok` |
| GPU probe fail | Mock timeout/nonzero/parse tests | `gpu_probe_failed`, other checks remain |
| Copy redaction | novels/prompt fixtures + path redaction unit tests | body/prompt absent; home dirs → `<redacted-path>` |
| Clipboard fallback | `copyDiagnosticsSummary` throws `NotAllowedError` | returns `fallback` and shows textarea |

## DoD

Browser health page detects GPU / Ollama / Milvus / SQLite. v0.1.0 Foundation & Local Runtime is accepted when this checklist plus parent Issues CY-70–CY-74 are Done.
